{
  "name": "@agent-dock/pi-extension-compat-spike",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "spike": "node src/run-spike.ts",
    "test": "vitest --run",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm run spike"
  },
  "dependencies": {
    "@agent-dock/protocol": "0.1.0",
    "@agent-dock/sandbox-supervisor": "0.1.0",
    "@earendil-works/pi-coding-agent": "0.80.10"
  }
}
