{
  "name": "@agent-dock/pi-embedded-rehydrate-spike",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "spike": "node src/run-spike.ts",
    "density": "node --expose-gc src/run-density-probe.ts",
    "runtime-benchmark": "node src/run-runtime-benchmark.ts",
    "live-model": "node src/run-live-model-probe.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest --run"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.84.1",
    "undici": "8.10.0"
  }
}
