{
  "format": "pi-cloud.fault-eval.v1",
  "description": "Deterministic infrastructure fault injections. Each case must prove the named failure is contained or recovered without relying on an external model.",
  "cases": [
    {
      "id": "duplicate-command",
      "workspace": "@pi-cloud/sandbox-supervisor",
      "file": "test/agent-run-supervisor.test.ts",
      "test": "deduplicates the same command and reuses one execution promise",
      "invariant": "At-least-once delivery does not execute the same command twice."
    },
    {
      "id": "stale-fencing-token",
      "workspace": "@pi-cloud/sandbox-supervisor",
      "file": "test/agent-run-supervisor.test.ts",
      "test": "retains the high-water fence after a pre-start release",
      "invariant": "A released or recovered old worker cannot reacquire an obsolete fence."
    },
    {
      "id": "message-stream-independence",
      "workspace": "@pi-cloud/pi-session-postgres",
      "file": "test/cloud-agent-runtime.test.ts",
      "test": "commits a complete assistant message independently of public event delivery",
      "invariant": "A streaming-path failure cannot roll back a complete Pi message already committed to SessionStorage."
    },
    {
      "id": "interrupted-visible-prefix",
      "workspace": "@pi-cloud/pi-session-postgres",
      "file": "test/cloud-agent-runtime.test.ts",
      "test": "keeps a failed visible assistant prefix in the next native Pi context",
      "invariant": "A browser-visible failed prefix remains model-visible after Worker replacement."
    },
    {
      "id": "terminal-stream-independence",
      "workspace": "@pi-cloud/control-plane",
      "file": "test/control-plane-api.test.ts",
      "test": "settles a completed backend execution without consulting the live projection",
      "setupTests": ["creates a project and workspace atomically, then creates a cold session"],
      "invariant": "A successful Run terminal commit does not reconstruct a complete message from the short-lived delta tail."
    },
    {
      "id": "control-plane-process-sigkill",
      "workspace": "@pi-cloud/sandbox-supervisor",
      "file": "test/control-channel-process-fault.test.ts",
      "test": "keeps an active assignment alive while the Control Plane process is killed and replaced",
      "invariant": "A Control Plane process replacement does not revoke a healthy fenced Agent Loop."
    },
    {
      "id": "checkpoint-corruption-and-cas",
      "workspace": "@pi-cloud/control-plane",
      "file": "test/checkpoint-store.test.ts",
      "test": "commits artifacts under a lease, cold-loads them, and rejects stale or corrupt state",
      "invariant": "Checkpoint integrity and compare-and-swap fencing survive cold recovery."
    },
    {
      "id": "cancel-complete-race",
      "workspace": "@pi-cloud/control-plane",
      "file": "test/control-plane-api.test.ts",
      "test": "records cancellation as too late when natural completion wins before its ACK",
      "setupTests": ["creates a project and workspace atomically, then creates a cold session"],
      "invariant": "Exactly one terminal Run outcome wins a cancel/complete race."
    },
    {
      "id": "stale-dispatch-claim",
      "workspace": "@pi-cloud/control-plane",
      "file": "test/control-plane-api.test.ts",
      "test": "fences a dispatcher whose pre-ACK claim lease was superseded",
      "setupTests": ["creates a project and workspace atomically, then creates a cold session"],
      "invariant": "A superseded dispatcher cannot start external work."
    },
    {
      "id": "orphan-runtime-cleanup",
      "workspace": "@pi-cloud/control-plane",
      "file": "test/control-plane-api.test.ts",
      "test": "terminates expired and orphan runtimes before settling a lost assignment",
      "setupTests": ["creates a project and workspace atomically, then creates a cold session"],
      "invariant": "Expired ownership is reconciled only after the orphan runtime is terminated."
    }
  ]
}
