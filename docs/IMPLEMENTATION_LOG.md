# Historical implementation log

The development log was removed from the current documentation set on
2026-08-16. It described many mutually exclusive pre-release architectures,
including Docker/gVisor, Temporal, execution Cells, Worker affinity,
MinIO/S3 Session checkpoints and Kopia Workspace copies.

The full log remains available in Git history for retrospective study. It is
not a source of current product behavior.

Use these maintained sources instead:

1. [documentation map](README.md);
2. [architecture](ARCHITECTURE.md);
3. [current ADR index](adr/README.md);
4. [run lifecycle](RUN_LIFECYCLE.md) and [threat model](THREAT_MODEL.md).
